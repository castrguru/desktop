/* Import modules. */
import { createApp } from 'vue'

/* Import (main) styles. */
import './main.css'

/* Import application. */
import App from './App.vue'

/* Create application. */
createApp(App).mount('#app')
